import React from 'react';
import { Button } from 'reactstrap';

export default class ModalButton extends React.Component {
  render() {
    return (
      <div>
        <Button color="primary" >Create Challenge</Button>{' '}
      </div>
    );
  }
}